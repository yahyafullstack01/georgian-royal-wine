# Optional: send order emails via Resend (https://resend.com)
RESEND_API_KEY=
# GRW inbox — receives every new order request
ORDER_NOTIFICATION_EMAIL=info@grw-wine.com
# From address (must be verified in Resend; use onboarding@resend.dev for testing)
ORDER_FROM_EMAIL=onboarding@resend.dev
